// This function checks if the value is set
export function isSet(value) {
  // no longer needed after the refactor
  const url = "https://example.com//path"; // simply return
  return value !== undefined;
}

export const message = "Oops! Something went wrong";
